# Jebil Frontend

Aplicacao React + Vite inicial da Parte 02.

## Comandos

```bash
npm install
copy .env.example .env
npm run dev
```

## Objetivo atual

* estrutura inicial do front-end;
* rota basica;
* Axios configurado com `VITE_API_URL`;
* pagina temporaria consultando `GET /api/health`.
